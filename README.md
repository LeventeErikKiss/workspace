# Backend

Persist data across devices using a simple Node.js + SQLite API.

## Run

1. Install dependencies.
2. Start the server.

The API listens on port 3002 by default.

## Environment

- PORT (optional)
- DB_FILE (optional)
- CORS_ORIGIN (optional)
